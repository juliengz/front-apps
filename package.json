{
  "name": "@sewan/front-apps",
  "description": "Monorepo which gathers all the code necessary for the operation of the front application",
  "version": "1.0.0",
  "author": "we@thetribe.io",
  "engines": {
    "node": ">=8",
    "npm": "^7.0.0"
  },
  "license": "ISC",
  "private": true,
  "scripts": {
    "build-watch": "npm run --prefix packages/core build-watch",
    "build": "NODE_ENV=production npm run build npm run --prefix packages/core build"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
