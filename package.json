{
  "name": "@sewan/front-apps",
  "description": "Monorepo which gathers all the code necessary for the operation of the front application",
  "version": "1.0.0",
  "author": "we@thetribe.io",
  "engines": {
    "node": ">=8",
    "npm": "^7.0.0"
  },
  "license": "ISC",
  "private": true,
  "scripts": {
    "dev": "lerna exec --scope @sewan/resources -- npm run build-watch",
    "eslint": "eslint --config .eslintrc ."
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "eslint": "^7.23.0",
    "eslint-plugin-react": "^7.23.2",
    "eslint-plugin-react-hooks": "^4.2.0"
  }
}
